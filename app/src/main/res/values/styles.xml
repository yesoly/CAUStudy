<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#0075BB</item>
        <item name="colorPrimaryDark">#005686</item>
        <item name="colorAccent">#0093EC</item>
    </style>

    <style name="ToggleButton.CustomTheme" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@drawable/button_t</item>
        <item name="colorControlActivated">@drawable/button_t_select</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="SplashTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
